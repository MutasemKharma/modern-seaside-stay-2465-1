# Development (SQLite)
# Used by prisma/schema.dev.prisma during local development
# Creates a local SQLite file in the project root
DATABASE_URL="file:./dev.db"

# Production (Supabase Postgres)
# Replace the placeholders with your Supabase credentials when deploying.
# Prisma will use the pooled (pgBouncer) URL for the main client, and the direct URL for migrations/maintenance.
# Example formats:
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_HOST.supabase.co:6543/postgres?pgbouncer=true&connection_limit=1"
# DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_HOST.supabase.co:5432/postgres?schema=public"

# Uncomment and set these in your production environment:
# DATABASE_URL=""
# DIRECT_URL=""

# --- Supabase JS (client-side) ---
# Used by the app in the browser to send requests directly to Supabase
VITE_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_ANON_PUBLIC_KEY"